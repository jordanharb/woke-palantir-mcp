/**
 * Placeholder entrypoint for environments that auto-detect an MCP TypeScript server.
 *
 * Note: This project serves MCP via Next.js at `app/mcp/route.ts` using mcp-handler.
 * This file exists only to satisfy entrypoint scans and is not used at runtime.
 */
export {};

